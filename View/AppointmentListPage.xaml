<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:ProjectMaui.ViewModels"
             x:Class="ProjectMaui.View.AppointmentListPage"
             Title="Lịch hẹn của tôi">

    <Grid RowDefinitions="Auto,*" Padding="10">

        <Frame Grid.Row="0" 
               BorderColor="#E0E0E0" 
               BackgroundColor="White"
               CornerRadius="10"
               Padding="10"
               HasShadow="True"
               Margin="0,0,0,10">
            <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">
                <Label Grid.Column="0" 
                       Text="Lọc:" 
                       VerticalOptions="Center"
                       FontSize="16"
                       TextColor="#555"/>
                <Picker Grid.Column="1"
                        SelectedItem="{Binding SelectedFilter}"
                        FontSize="16">
                    <Picker.Items>
                        <x:String>Tất cả</x:String>
                        <x:String>Chờ xác nhận</x:String>
                        <x:String>Đã xác nhận</x:String>
                        <x:String>Hoàn thành</x:String>
                        <x:String>Đã hủy</x:String>
                    </Picker.Items>
                </Picker>
                <Button Grid.Column="2"
                        Text="🔄"
                        Command="{Binding RefreshCommand}"
                        BackgroundColor="#3498DB"
                        TextColor="White"
                        WidthRequest="50"
                        HeightRequest="40"
                        CornerRadius="8"/>
            </Grid>
        </Frame>

        <ActivityIndicator Grid.Row="1"
                          IsRunning="{Binding IsLoading}"
                          IsVisible="{Binding IsLoading}"
                          Color="#3498DB"
                          VerticalOptions="Center"
                          HorizontalOptions="Center"/>

        <CollectionView Grid.Row="1" 
                       ItemsSource="{Binding FilteredAppointments}"
                       IsVisible="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}">

            <CollectionView.EmptyView>
                <VerticalStackLayout VerticalOptions="Center" 
                                    HorizontalOptions="Center"
                                    Spacing="10">
                    <Label Text="" 
                           FontSize="60"
                           HorizontalOptions="Center"/>
                    <Label Text="Chưa có lịch hẹn nào" 
                           FontSize="18"
                           TextColor="#999"
                           HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="0,5" 
                           Padding="15"
                           BackgroundColor="White"
                           BorderColor="#E0E0E0"
                           CornerRadius="15"
                           HasShadow="True">

                        <Grid RowDefinitions="Auto,Auto,Auto,Auto" RowSpacing="10">
                            <Grid Grid.Row="0" ColumnDefinitions="*,Auto">
                                <Label Grid.Column="0"
                                       Text="{Binding AppointmentDateDisplay}" 
                                       FontSize="18"
                                       FontAttributes="Bold"
                                       TextColor="#2C3E50"/>

                                <Frame Grid.Column="1"
                                       BackgroundColor="{Binding StatusColor}"
                                       Padding="10,5"
                                       CornerRadius="15"
                                       HasShadow="False">
                                    <Label Text="{Binding Status}" 
                                           FontSize="12"
                                           TextColor="White"
                                           FontAttributes="Bold"/>
                                </Frame>
                            </Grid>

                            <VerticalStackLayout Grid.Row="1" Spacing="5">
                                <Label FontSize="16" TextColor="#2C3E50">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="Bác sĩ: " FontAttributes="Bold"/>
                                            <Span Text="{Binding DoctorName}"/>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>

                                <Label FontSize="14" TextColor="#7F8C8D">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="" />
                                            <Span Text="{Binding DepartmentName}"/>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                            </VerticalStackLayout>

                            <VerticalStackLayout Grid.Row="2" Spacing="5">
                                <Label FontSize="14" TextColor="#7F8C8D">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="" />
                                            <Span Text="{Binding PatientName}"/>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>

                                <Label FontSize="14" TextColor="#7F8C8D">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="" />
                                            <Span Text="{Binding PatientPhone}"/>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                            </VerticalStackLayout>

                            <Frame Grid.Row="3" 
                                   BackgroundColor="#F8F9FA"
                                   Padding="10"
                                   CornerRadius="8"
                                   HasShadow="False"
                                   IsVisible="{Binding Reason, Converter={StaticResource StringNotEmptyConverter}}">
                                <Label Text="{Binding Reason}" 
                                       FontSize="14"
                                       TextColor="#555"
                                       LineBreakMode="WordWrap"/>
                            </Frame>

                            <Grid Grid.Row="3" 
                                  ColumnDefinitions="*,*" 
                                  ColumnSpacing="10"
                                  Margin="0,10,0,0">

                                <Button Grid.Column="0"
                                        Text="Xem chi tiết"
                                        BackgroundColor="#3498DB"
                                        TextColor="White"
                                        CornerRadius="8"
                                        HeightRequest="40"
                                        FontSize="14"/>

                                <Button Grid.Column="1"
                                        Text="Hủy lịch"
                                        BackgroundColor="#E74C3C"
                                        TextColor="White"
                                        CornerRadius="8"
                                        HeightRequest="40"
                                        FontSize="14"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:AppointmentListViewModel}}, Path=CancelAppointmentCommand}"
                                        CommandParameter="{Binding AppointmentId}"
                                        IsVisible="{Binding Status, Converter={StaticResource StatusToCancelButtonVisibilityConverter}}"/>
                            </Grid>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>